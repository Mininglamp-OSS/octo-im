name: Docker Lint

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile*'
      - 'web/Dockerfile*'
      - 'docker/**'
      - '.hadolint.yaml'
      - '.github/workflows/docker-lint.yml'
  pull_request:
    branches: [main]
    paths:
      - 'Dockerfile*'
      - 'web/Dockerfile*'
      - 'docker/**'
      - '.hadolint.yaml'
      - '.github/workflows/docker-lint.yml'

jobs:
  docker-lint:
    uses: Mininglamp-OSS/.github/.github/workflows/reusable-docker-lint.yml@v1
    with:
      dockerfiles: 'Dockerfile web/Dockerfile'
